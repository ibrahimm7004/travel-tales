from typing import List


def duplicate_filter(files: List[str]) -> List[str]:
    return files


def quality_filter(files: List[str]) -> List[str]:
    return files


def select_highlights(files: List[str], n: int = 40) -> List[str]:
    return files[:n]
