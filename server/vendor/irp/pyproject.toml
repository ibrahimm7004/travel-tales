[project]
name = "irp-vendor"
version = "0.0.1"
description = "Vendored TravelTales IRP snapshot"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["irp"]




